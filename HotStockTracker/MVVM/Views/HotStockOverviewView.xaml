<mah:MetroContentControl
    x:Class="HotStockTracker.MVVM.Views.HotStockOverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HotStockTracker.MVVM.Views"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding DayItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Padding="10" BorderBrush="{DynamicResource MahApps.Brushes.Accent}" BorderThickness="1" Width="250">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding Date, StringFormat='dd.MM.yyyy'}" 
                                           FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>

                                <Grid Grid.Row="1" Margin="0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <DataGrid Grid.Column="0" ItemsSource="{Binding TopGainers}" 
                                              AutoGenerateColumns="False" HeadersVisibility="Column"
                                              IsReadOnly="True" SelectionMode="Single">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                                            <DataGridTextColumn Header="Wzrost %" Binding="{Binding ChangePercent, StringFormat={}{0}%}" Width="0.5*"/>
                                            <DataGridTextColumn Header="Obrót" Binding="{Binding Volume}" Width="0.5*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <DataGrid Grid.Column="1" ItemsSource="{Binding TopLosers}" 
                                              AutoGenerateColumns="False" HeadersVisibility="Column"
                                              IsReadOnly="True" SelectionMode="Single">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                                            <DataGridTextColumn Header="Spadek %" Binding="{Binding ChangePercent, StringFormat={}{0}%}" Width="0.5*"/>
                                            <DataGridTextColumn Header="Obrót" Binding="{Binding Volume}" Width="0.5*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>

                                <Grid Grid.Row="2" Margin="0,10">
                                    <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" 
                                               Visibility="{Binding IsNotCurrentDay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <TextBox Text="{Binding Summary, UpdateSourceTrigger=PropertyChanged}" 
                                             Visibility="{Binding IsCurrentDay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                             mah:TextBoxHelper.Watermark="Wpisz podsumowanie dnia..."/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="10">
            <TextBlock Text="Sekcja dodawania gorących spółek - do implementacji" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</mah:MetroContentControl>